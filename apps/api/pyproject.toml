[project]
name = "prcrew"
version = "0.1.0"
description = "FastAPI + LangGraph backend for pr-crew, a multi-agent GitHub PR reviewer."
readme = "README.md"
authors = [
    { name = "Sai Praveen Tatiparthi", email = "saipraveen.tatiparthi@sjsu.edu" }
]
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.122.0",
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "langgraph>=1.2.11",
    "pydantic>=2.13.4",
    "slowapi>=0.1.10",
    "sse-starlette>=3.4.8",
    "uvicorn>=0.52.3",
]

[build-system]
requires = ["uv_build>=0.11.16,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "respx>=0.23.1",
    "ruff>=0.16.3",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = ["."]
